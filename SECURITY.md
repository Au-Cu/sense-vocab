# 安全说明

## 已实施

- Supabase 行级安全和受限 RPC；
- 乐观并发版本校验，避免多设备静默覆盖；
- 私有反馈图片存储桶和短时签名 URL；
- 反馈提交频率、数量、文件类型和路径校验；
- 浏览器端图片解码与重新编码，移除 EXIF 和非图像载荷；
- CSP、HSTS、禁止嵌入、MIME 嗅探防护和最小浏览器权限；
- 构建前内容身份校验和来源权利审计；
- 管理后台不在前端导航中展示，并标记禁止搜索引擎索引。

## 运营要求

- 管理员账户必须启用 MFA，并使用独立强密码；
- Supabase 和 Cloudflare 账户均应启用 MFA；
- 不得把 service-role key、数据库密码或管理员令牌写入前端或仓库；
- 至少每月检查管理员访问日志、异常登录和反馈存储；
- 至少每季度验证备份可恢复性；
- 发现漏洞可通过应用内反馈提交，不应在反馈中附带真实用户数据。

## 漏洞报告

请说明受影响页面、复现步骤、影响范围和建议修复方式。不要访问、修改或公开其他用户数据。
正式公开运营前应补充独立的安全联系邮箱和漏洞响应时限。
