# Sense Vocab 当前状态

更新时间：2026-08-22（Asia/Hong_Kong）

本文件只记录会变化的当前快照。长期规则见 `PROJECT_CONTEXT.md`，历史原因见 `DECISIONS.md`。

## 发布与仓库

| 项目 | 当前值 |
| --- | --- |
| 正式版本 | `1.7.1` |
| 分支 | `main` |
| 最新标签 | `v1.7.1` |
| 公网 | `https://sense-vocab.pages.dev/` |
| 后台 | `https://sense-vocab.pages.dev/admin.html`，无前台入口 |
| 工作区 | `D:\Files\sense-vocab-mvp` |

`v1.3.0` 后的 2026-08-05 改动已统一收敛为 `v1.3.1`。历史提交中曾出现 `1.3.2` 文案，但没有保留 `v1.3.2` 标签或 Release；不要通过改写历史修正它。

2026-08-09 的聚合版本已部署到 `https://f5a2750f.sense-vocab.pages.dev`，正式域名已验证。该版本汇总结构化义项反馈、单词卡前后浏览、易混词跨设备同步、移动端单词列表高度修正、反馈提交后返回词卡、合并结果双端持久化以及本轮词库反馈校正。

2026-08-09 OP 日间修复已通过隔离构建部署到 `https://c8215e41.sense-vocab.pages.dev` 并同步至正式域名：移动端单词列表额外预留浏览器控件净空；反馈成功后关闭账户层返回原页面；同一义项的并发状态按精确更新时间、学习日和最后学习日期选择较新证据，并回读确认本地与云端一致；强化重置保留早间复习已确认项；v1.4.0 新增的 10 个义项会为已学单词补入待新学队列。部署包以 `v1.4.0` 为干净基线，仅包含 4 个生产文件的 OP 修复，不含本页下述尚未部署的 LC 合规改动。工作区构建通过，完整测试为 95/95；公网目标回归通过。

2026-08-08 已应用 Supabase 迁移 `20260808150845_confusion_links_account_sync_guard.sql`：易混词关系继续使用现有账户快照作为唯一权威数据源，并补齐跨设备校验、异常删减保护和删除墓碑合并；迁移未批量改写用户学习数据。当前构建通过，完整测试为 88/88；内容身份、语义、双语、上下文例句和常见义覆盖阻断项均为 0。

2026-08-10 已部署商业合规数据库控制及管理员合规看板。Supabase 已应用 `20260809060413_compliance_release_controls.sql`、`20260809153605_compliance_issue_cards.sql` 及两份追加运行时修复迁移；本地与远端迁移历史一致，`public`/`private` schema 的远端函数 lint 为 0，匿名调用管理员合规 RPC 返回 `42501`。后台现在按问题显示横向红/黄/绿卡片，保留追加式问题与总体发行结论快照、操作账号、变更时间、下一步方案、筛选、CAS 冲突保护和审计记录；不自动作出法律意见。前端已部署到 `https://eb13dbb6.sense-vocab.pages.dev` 并同步正式域名，关键后台资源与本地 `dist` 的 SHA-256 一致。`process-feedback-retention` Edge Function、Vault secret、cron 实际执行、v3 重新同意和删除链路仍未完成端到端生产验证。

2026-08-12 已部署并验证本轮反馈内容修复与运行维护：33 个稳定内容项共 155 个修改字段通过人工审核及变更集权利门禁，新增 11 个义项且保留全部既有内容身份；移动视口变化后会重新约束单词标题字号。Cloudflare 部署 `https://fa489303.sense-vocab.pages.dev` 与正式域名的关键运行资源及合规产物均和 `dist` 的 SHA-256 一致。Supabase `process-feedback-retention` Edge Function 已通过 API 打包部署为 active v1，匿名请求返回 `401`；每日 cron 已启用，但两项 Vault secret 均未配置，当前 3 次运行全部记录为 `skipped_missing_secret`，未触发清理，反馈记录总数保持不变。删除链路、v3 重新同意仍未完成端到端生产验证。

2026-08-12 OP 日间修复已应用 Supabase 迁移 `20260812051224_increase_feedback_submission_limits.sql`，把单账户反馈限额提高到每小时 20 次、每天 60 次，保留账户级串行锁及原有 RPC 权限边界，未改写或删除反馈和用户数据。移动端反馈表单的可聚焦控件固定为至少 16px，避免 iOS 浏览器自动放大；Cloudflare production deployment `https://521d9980.sense-vocab.pages.dev` 与正式域名的 `index.html`、`styles.css` 均和 `dist` 的 SHA-256 一致。`npm run build:web`、Supabase 安全审计和 102/102 完整测试通过。

2026-08-12 R&D 已纠正并重新部署多发音功能：此前界面具备连续播放能力，但正式词库实际只覆盖少量双录音，不能据此宣称功能完成。现按义项、词性和实际读音核对 56 个异读词的 112 种读音，其中 110 种绑定了相互独立且具备 Wikimedia Commons 作者、许可证、来源页、访问时间和证据哈希的录音；130 个义项新增或修正了录音绑定，`aggregate` 的名词/形容词与动词音标也已分开。`decrease` 的名词 `/ˈdikris/` 和 `insult` 的动词 `/ɪnˈsʌlt/` 尚无可独立验证的专属录音，保持为空并记为 `evidence_pending`，不拿其他读音冒充。自动、手动和教程播放会按归一化后的实际读音依次播放全部独立录音，同一读音或 URL 只播放一次，单条失败时继续；词卡标题和录音来源区不再列音标，音标只保留在各义项内。桌面与 390×844 手机实页验收通过，内容身份仍为 6,607 词、10,235 义项、2 本词书，远程音频缺署名数为 0；`npm run build:web` 与 104/104 完整测试通过。Cloudflare production deployment `https://ff0e7607.sense-vocab.pages.dev` 已同步正式域名，首页、`app.js`、词库索引和词库 bundle 均与已验证 `dist` 的 SHA-256 一致。

2026-08-12 CD 已通过追加迁移 `20260812154800_update_compliance_dashboard_latest_evidence.sql` 把最新内容清洁候选、重点复核和多发音权利证据同步到生产合规看板：7 张既有问题卡新增 #2 快照，16 张原始 #1 快照全部保留；商业发行结论新增 #2 快照并继续为 `not_releasable`。看板明确记录 24,583 个清洁目标字段中已有 21,848 个候选、2,735 个 `evidence_pending`，455 个重点字段已复核但仍待用户终审、未应用、未商业清关；Wikimedia 音频 `CLEARED` 范围只更新到当前 1,747 个已绑定且有逐项证据的运行时字段，两种缺失专属录音仍不在范围内。生产迁移历史一致，远端数据库 lint 无错误，匿名角色无看板或追加权限，普通 authenticated 仍由管理员校验以 `42501` 拒绝；本次没有修改运行时词库、身份锁、用户学习数据或反馈状态。证据见 `artifacts/compliance/compliance-dashboard-evidence-2026-08-12.json`。

2026-08-12 R&D 已完成供 CD 独立调用的本地批量内容工具链，当前仅保留为未部署的工作区工具：严格英文到中文批处理入口使用一次模型加载、token 长度分桶、失败批次递归拆分、稳定内容 ID 映射、绑定模型资产/解码参数/规则版本的内容寻址缓存及原子 checkpoint；模型或语言方向不可用时明确阻断，不回退到历史非官方接口。例句匹配新增持久 embedding 缓存，并把义项×候选及相邻义项排除评分矩阵化，不改变原选择阈值。固定 24 项真实匿名基准在 8 核 Snapdragon 8cx Gen 3 上三轮温态中位加速 `4.15x`，逐项输出哈希完全一致；128×256 压力评分基准加速 `65.94x`，排序完全一致，最大数值差 `8.26e-8`。真实运行时小组评分受 Python 组装开销影响不保证单组加速，主要收益来自 embedding 跨作业缓存和候选规模增长后的矩阵路径。20 项针对性回归覆盖缓存失效、缓存损坏、批次/单项失败、数量不匹配、模型缺失、严格无网络回退和中断续跑；未修改正式词库、用户数据、版本或部署。使用说明见 `docs/CONTENT_BATCH_TOOLS.md`，原始证据见 `artifacts/benchmarks/content-batch-current.json`。

2026-08-13 OP 已实施并部署用户全部批准的内容反馈批次 `OP-FB-2026-08-13-B`：13 个稳定内容项共 81 个字段通过变更集、身份和字段级权利链门禁，其中新增 9 个义项，运行时规模变为 6,607 词、10,244 义项、2 本词书。新增义项继续绑定稳定 `wordId`/`senseId`、词性、synset 或明确的无 synset 结论；对已引入单词显式初始化为待新学。学习队列会把同词的到期复习与新增义项合并为一张可操作卡片，避免新增义项或强化重置后的义项呈现白底且无法选择。Cloudflare production deployment `https://032c44f9.sense-vocab.pages.dev` 与正式域名的首页、`app.js`、样式、词库索引、词库 bundle、公开权利摘要和 SBOM 均与最终 `dist` 的 SHA-256 一致；公网反馈返回词卡、多发音和新增义项桌面/手机回归 3/3 通过。最终 `npm run build:web`、106/106 完整浏览器测试、20/20 Python 批处理测试、两份内容变更集、Supabase 安全审计、远端迁移历史和数据库 lint 均通过。当前批次 81 个字段全部为 `CLEARED`，但历史全局商业门禁仍有 10,218 个 `BLOCKER`，不构成收费商业发行清关。

2026-08-13 OP 已修复账户刷新和学习天数口径并完成来源署名迁移。账户刷新时先确认身份并切换到账户专属本地缓存，确认前不显示可能属于游客缓存的计划和进度卡片；云端收敛继续回写账户本地缓存和云端，首页操作与首次教程在认证停滞时仍可使用。单词列表的学习天数改为统计该词实际发生学习活动的去重日期，不再按首次到完成之间的自然日跨度计数。学习卡不再常驻渲染发音和例句来源长文本；无需登录的“条款、隐私与内容来源”页面提供可搜索、可分页、桌面和手机可用的逐资产目录，覆盖 1,747 个音频绑定（按 URL 去重为 1,204 项）、944 个 Tatoeba 绑定（去重为 936 项）及 765 个可追溯的 Wiktionary/Kaikki 绑定。Cloudflare production deployment `https://2b8e55ff.sense-vocab.pages.dev` 与正式域名的 8 个关键资源均和最终 `dist` 的 SHA-256 一致，正式域名 4 条针对性用户路径通过；`npm run build:web` 与 110/110 完整浏览器测试通过。迁移没有改写内容身份、用户学习数据、反馈或权利结论，历史全局 10,218 个 `BLOCKER` 保持不变。

2026-08-13 OP 随后修复移动端词卡切换后的滚动位置残留：渲染层只在词卡身份变化或进入完成态时重置文档滚动，同一词内显示义项、完成选择和展开详情不会被强制回顶。Cloudflare production deployment `https://f13880c6.sense-vocab.pages.dev` 与正式域名的 `index.html`、`app.js` 均和最终 `dist` 的 SHA-256 一致，正式域名 390×844 长词卡滚到底部后切换下一词的回顶回归通过；`npm run build:web` 与 111/111 完整浏览器测试通过。

2026-08-14 OP 已实施用户批准的内容反馈批次 `OP-FB-2026-08-13-C`：新增稳定义项 `volunteer:n-3`，并同步挂入考研、雅思两本词书；修正 `visual:adj-1` 的例句及译文。新义项对已引入单词显式初始化为可选择的待新学状态，既有义项状态保持不变。批次 10 个字段全部通过变更集、内容身份和字段级权利链门禁，运行时规模变为 6,607 词、10,245 义项、2 本词书；历史全局 10,218 个 `BLOCKER` 保持不变。Cloudflare production deployment `https://76cc39b8.sense-vocab.pages.dev` 与正式域名的 10 项关键资源均和最终 `dist` 的 SHA-256 一致；正式域名桌面与 390×844 手机词卡确认 `volunteer:n-3`、`visual:adj-1` 和既有 `present:adj-6` 正常可见。`npm run build:web`、113/113 完整浏览器测试及内容专项审计通过，3 条对应反馈已在原状态仍为 `new` 时条件更新为 `resolved`，当前后台没有 `new` 或 `in_progress` 反馈。

2026-08-22 OP 以 `v1.7.1` 回补截止 2026-08-14 的已审核内容反馈批次 `op-feedback-2026-08-14`：22 个稳定内容项、84 个批准字段已应用，包含 12 个新增义项和 24 个身份字段；`F-06` 因证据待补未生成、未应用。新增义项按候选记录分别保留考研或雅思词书引用，已学习用户通过显式待新学初始化兼容；既有 wordId、senseId、词书范围和用户学习数据未被删除或重写。运行时规模变为 6,607 词、10,257 义项、2 本词书；内容身份、变更集、字段级权利链、`npm run build:web`、113/113 完整浏览器测试和内容专项审计通过，历史全局商业阻断仍保留为 10,217 个 `BLOCKER`。截止日之后的新反馈未纳入本批次。

2026-08-22 OP 继续复盘同设备游客记录与账户记录合并后丢失当天学习进度的问题，确认此前部署到 `https://1c73662c.sense-vocab.pages.dev` 的保护仍不完整。根因是每日初始化让数日前的游客档案再次被误判为待合并“本机记录”；冲突提示冻结了登录时的游客与云端快照，提示存在期间用户仍可学习但账户同步被阻断，最终合并又用两份冻结旧快照覆盖当前账户状态。受影响当天的逐词选择没有进入数据库保留快照，用户导出的本机恢复文件同样是较旧游客档案，因此没有足够证据精确回写当天逐义项状态，本轮未执行生产用户数据写入。当前修复改为：按真实日期证据筛选游客增量，空会话和每日推导活动不再触发冲突；游客提示期间账户本地与云端继续同步；合并时重新读取最新云端，并与当前实时账户状态、经证据筛选的游客增量三路收敛后回读验证。界面明确标注“历史游客记录”，不再把它冒充账户本地缓存。启动阶段新增独立全屏加载页，在词库和账户作用域均就绪前以原生 `hidden`、`inert` 和 `aria-hidden` 完全隐藏并禁用主页交互。同步专项 39/39、最终 `npm run build:web`、119/119 完整浏览器测试及正式域名 390×844 慢云端启动实页验证通过；Cloudflare production deployment `https://8805d9e4.sense-vocab.pages.dev` 已同步正式域名，`index.html`、`styles.css`、`app.js`、`account.js`、`sync-state.js` 均与最终 `dist` 的 SHA-256 一致。历史全局 10,217 个 `BLOCKER` 保持不变。

## 当前规模

运行时词库 `data/vocabulary-bundle.json`：

| 范围 | 单词 | 义项 |
| --- | ---: | ---: |
| 共用池 | 6,607 | 10,257 |
| 考研词汇 | 5,042 | 7,940 |
| 雅思词汇 | 4,827 | 7,570 |

考研词汇是默认词书。两本词书共享内容身份，但学习状态、计划和历史相互独立。

## 当前能力

- 义项级新学、强化、复习与 double check 状态机。
- 真实日期、学习日、跨午夜学习窗口和活动热力图。
- 每本词书独立的计划、列表、筛选、排序和统计。
- 游客本地记录、账户云同步、离线缓存、CAS 冲突检测、版本向量合并、双端持久化回读和恢复保护。
- 邮箱 OTP、一次性邀请码、会员期限、公告、双向反馈和管理后台。
- 手机教程、移动端布局、可随账户跨设备同步的易混词球体及词卡跳转。
- 词形、义项内音标、在线音频及浏览器语音回退；异读词连续播放全部已核验的独立录音，缺少专属录音时明确留空而不冒充。
- 单词列表只读词卡按进入时的搜索、筛选和排序前后浏览；学习回看支持逐词双向移动。
- 学习页反馈支持结构化问题类型，并按类型填写缺失义项、其他问题说明或选择具体义项。
- 公共内容来源页支持按单词、稳定内容 ID、作者、许可证和来源类型查询需要署名的运行时资产，学习卡不重复常驻展示来源长文本。
- 管理员合规看板支持法律风险与第三方权利链卡片、当前发行结论、人工追加快照、历史追溯、下一步方案和多维筛选；颜色与文字标签并用，严重度与处理进度分离。
- CD 本地批量内容工具支持稳定 ID 作业映射、严格离线批量翻译、内容寻址缓存/断点续跑、持久 embedding 缓存和矩阵化例句匹配；工具只产出候选或审核文件，不直接应用正式内容。

## 自动化

- 配置：`C:\Users\周金铜\.codex\automations\sense-vocab\automation.toml`
- 状态：`ACTIVE`
- 时间：每天 23:30，香港时区。
- 规则：先检查同一工作区的任务状态租约；发现其他任务仍 active 时事件式等待，总上限 30 分钟，不通知、不唤起、不高频轮询。租约释放后重新检查工作树，再执行新建/未处理反馈门禁；通过后按香港自然日最多发布一个 GitHub 版本。知识内容反馈确需生成替换内容时，按 D-018 先整理、去重当前检查中全部待生成项目，再最多向既有 CD 对话发送一次匿名化批量候选请求，不得逐条递交；随后停止当次发布并等待用户逐项或逐字段明确审核，审核前不修改正式内容或反馈状态。日间修改只部署公网；GitHub Release 记录完整版本更新，README 只维护长期项目简介，历史 CHANGELOG 不再逐版追加。
- 流程分级：自动化 Prompt 已同步 D-020 与 D-021；对最终拟发布输入保留完整构建、测试和适用专项门禁，但不在外层重复 `build:web` 已覆盖的同输入内容身份、内容审计和权利台账检查，并主动按最小规范落点维护永久规则。参数化校验器、自动验证凭据和“部署已验证产物”入口仍未实现。
- 当前目标已经是 `OP｜运营维护` 对话，`target_thread_id` 为 `019fd28c-40a0-7b71-9ab0-a17ad7af8968`；不再投递到 Archived。

## 验证基线

- `package.json` 还提供 `npm run enrich:rights`、`npm run build:rights-ledger`、`npm run build:third-party-compliance`、`npm run audit:supabase-security` 和 `npm run verify:commercial-release`。
- D-020 的风险分级与同源结果复用原则已经生效；当前尚未实现通用的参数化变更集校验器、自动验证凭据、按哈希缓存、纯只读 `verify` 或“部署已验证 `dist`”入口。现阶段只能复用相关输入与命令结果均有明确证据且确实未变化的既有验证，不得把规划中的工具写成当前能力，也不得仅凭“今天跑过”跳过现有硬门禁。
- 当前正式发布记录的完整测试基线为 113/113；后续相关输入发生变化时仍需按 D-020 重新运行受影响测试，输入未变化时只能凭可核验证据复用。
- 当前工作树的完整测试为 115/115；`npm run build:web`、内容身份与内容审计、权利台账、公共来源覆盖检查、桌面/手机回归和公网资源哈希核对均通过。全局商业门禁仍按设计失败，不能据此发布收费版本。
- 词库身份锁、考研原始词书哈希和内容审计是内容变更的发布门禁。

## 当前风险与待办

1. 商业发行结论仍为不可发行：权利台账已改为默认拒绝，任何非空字段缺作者、许可证或来源链接即为 `BLOCKER`；当前 10,257 个义项中 40 个已清关，10,217 个仍为历史 `BLOCKER`，严格门禁不得绕过。
2. 1,747 条 Wikimedia 音频的作者、许可证和来源页缺口已清零；944 条 Tatoeba 记录与官方文本全部一致，原 12 条重复句末句点已保留前后哈希后规范化。当前审计仍有 314 条 quotation、85 条 semantic 来源缺口、8,902 条翻译来源商业复核记录及 2 个历史外部服务阻断。
3. `supabase@2.110.0` 与 `wordnet@2.0.0` 的精确安装包和对应上游版本均已验证 MIT 声明并保存哈希，直接依赖未验证许可证证据数为 0；上游与安装包客观上均没有独立 LICENSE 文件，该事实继续在 SBOM 和许可证包中披露，不以其他项目文本代替。
4. 合规数据库迁移、生产函数 ACL、每日保留 cron 与 `process-feedback-retention` Edge Function v1 已部署；Vault secret 尚未配置，cron 当前安全跳过，v3 重新同意和删除链路仍未完成端到端生产验证。
5. 运营主体、目标司法辖区、收费与退款规则、备案/许可、DPA 和跨境机制仍需书面闭合。以 `COMMERCIAL_RELEASE_CHECKLIST.md` 为准，不得把本地安全控制等同于完成商业合规。
6. 释义译文去除元语言冗余开头的规则已写入 `docs/departments/CD.md`，但未对历史词库进行字符串截断或未经逐项审核的整库改写；后续必须按稳定内容 ID 批量生成、逐项确认完整自然度并完成字段级权利链后再应用。

每次完成显著功能、数据结构、部署、自动化或风险状态变化后，更新本文件并写明日期。
