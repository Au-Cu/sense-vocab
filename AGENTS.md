# Sense Vocab 协作入口

本仓库是 Sense Vocab 的唯一正式工作区。所有项目对话开始工作前，先阅读：

1. `docs/PROJECT_CONTEXT.md`：长期有效的产品、数据和技术约束。
2. `docs/CURRENT.md`：当前版本、部署、自动化和待处理事项。
3. `docs/DECISIONS.md`：已经确定的关键决策及原因。
4. 当前对话对应的 `docs/departments/*.md` 部门章程。

## 信息优先级

发生冲突时，按以下顺序判断：

1. 当前代码、数据库迁移、自动化配置和可重复验证结果。
2. `docs/PROJECT_CONTEXT.md` 与已生效的 `docs/DECISIONS.md`。
3. `docs/CURRENT.md`。
4. `README.md`、`CHANGELOG.md` 和其他专项文档。
5. 已归档的 `Archived` 对话。它只用于追溯历史，不是当前事实源。

发现文档与实现不一致时，先验证实现，再同步修正文档；不要靠旧对话猜测当前行为。

## 五个部门

- `PMO｜项目管理办公室`：澄清需求、补足边界、讨论方案和输出可直接执行的 Prompt。见 `docs/departments/PMO.md`。
- `R&D｜新功能研发`：实现新功能、验证效果并做必要微调。见 `docs/departments/R&D.md`。
- `OP｜运营维护`：应急修复、用户反馈处理、运行维护和每日 GitHub 发布。见 `docs/departments/OP.md`。
- `CD｜内容研发`：词义、释义、例句、翻译、音标、词形和内容质量。见 `docs/departments/CD.md`。
- `LC｜法务合规`：审查适用法律法规、第三方内容与软件权利链、隐私和商业发行阻断项。见 `docs/departments/LC.md`。

用户通过打开哪个对话来选择部门。默认不要自动唤起、转交或要求其他部门同时响应；D-018 仅允许 OP 在知识内容反馈修复中向既有 CD 对话发送一次匿名化的批量候选内容生成请求。同一检查批次存在多项待生成内容时，必须先按稳定内容 ID 整理、去重并一次性交接，不得逐条发送；之后必须停止并等待用户人工审核。也不设置“某类文件只能由某部门修改”的硬边界，当前部门可以完成任务所必需的相邻改动。

## 共同工作规则

- 工作目录固定为 `D:\Files\sense-vocab-mvp`。不要修改承载自动化或其他项目的目录。
- 先检查 `git status`，保留并适配用户或其他对话已有的改动，不得擅自回滚。
- 同一时刻只允许一个写入者直接修改正式工作区。需要并行编码时使用独立 Git worktree，再明确合并；只读分析可以并行。
- 正式工作区使用基于 Codex 任务当前轮次状态的临时写入租约。任何任务开始修改文件、运行可能写入文件的命令、执行数据库迁移或部署后，直到所有工具和进程结束，都视为 `工作区租约：HELD`；结束本轮时必须明确报告 `工作区租约：RELEASED`。`RELEASED` 只表示本轮不再写入，不等于改动已经完成、验证或可发布。
- 用户学习数据、反馈、账户、公告和云端快照默认不可删除、批量重写或迁移。疑似数据丢失时先只读定位真实数据源，区分删除、查询、RLS、账户映射和同步显示问题。
- 不在日志、提交、文档或回复中泄露邮箱、用户反馈原文、令牌、密钥或其他敏感信息。前端只能使用 Supabase Publishable/Anon Key，绝不提交 Service Role Key。
- 单词 ID、义项 ID、词书引用和状态迁移属于兼容性边界。内容更新不得让既有用户无提示地丢失进度，或让已学单词突然出现无法解释的未学义项。
- 手工编辑使用 `apply_patch`。只改任务所需范围，提交前检查最终 diff，不默认使用 `git add -A`。

## 验证与发布

- 按改动风险运行针对性测试；产品发布至少运行 `npm run build:web` 与 `npm test`。
- 词库改动至少运行 `npm run verify:content-identity`、`npm run audit:content`，并按内容范围运行相应 Python 审计和字段级权利链检查。按 D-017，普通维护必须清零本次变更集的内容质量、身份兼容和权利链阻断，收费商业发行必须清零拟发行范围内全部未解决 `BLOCKER/HIGH`；未触及的历史权利阻断继续留档，但不冻结无关维护。
- 数据库改动使用追加迁移、RLS 和受控 RPC，先做 dry run 与兼容性评估；不得直接改写生产用户学习数据。
- 已验证的日间功能或修复可以立即部署公网，但不因此单独改版本号或创建 Git tag。
- 每日自动化在发布前先检查同一正式工作区的其他 Codex 任务。发现其他任务当前轮次仍为 active 时，不立即退出、不通知或唤起该任务，也不进行高频轮询；使用事件式等待，单次日更总等待上限为 30 分钟。任务需要用户处理、状态无法确认、等待超时或租约未明确释放时，停止发布并顺延；释放后仍须重新检查任务状态、`git status` 和完整 diff。
- GitHub 正式版本由 OP 按香港自然日聚合，每天最多一个。发布时同步 `package.json` 和 `package-lock.json`，通过反馈门禁、构建与测试后再创建 annotated tag 和 Release；GitHub Release 是正式版本更新与维护记录的唯一权威来源。
- `README.md` 始终作为长期有效的项目简介，只在产品定位、稳定核心能力、使用方法、架构、部署、安全或兼容性等长期事实变化时更新；不写逐版本发布要点，不要求与每个版本号机械同步。
- `CHANGELOG.md` 保留 `v1.3.1` 及以前的历史记录，后续不再逐版追加，也不是发布门禁。README 或其他项目文档仅发生事实漂移时，使用 docs 提交修正，不提高版本号、不移动已有 tag。

## 文档维护

- 长期产品或架构约束改变：更新 `PROJECT_CONTEXT.md`，必要时在 `DECISIONS.md` 增加决策。
- 当前版本、部署、自动化、风险或近期重点改变：更新 `CURRENT.md`。
- 部门职责或工作方式改变：只更新对应部门章程，避免复制到所有文件。
- 不把临时排查过程、一次性截图描述、用户隐私或已经失效的实现细节写入长期文档。
