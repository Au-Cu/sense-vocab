# 隐私与数据保留执行规则

版本：2026-08-09-v3

## 数据类别与期限

- 游客学习数据：仅保存在当前浏览器 localStorage，直到用户清除或导出。
- 账户、学习状态和通知：保留至账户删除或适用法律要求的期限；删除必须覆盖主数据、关联状态和可识别副本。
- 反馈文字与附件：未处理反馈原则上最长 365 天；进入 `resolved` 后，数据库触发器把到期日限制为 `resolved_at + 180 days`，不得因人工更新延长。
- 同意记录：保存文档类型、版本、同意时间和必要审计信息，用于证明同意与触发重新同意。
- 安全、认证、管理员审计和保留任务日志：按安全需要、合同和适用法律设定期限，不无限期保留。
- 备份和云端快照：删除请求进入备份自然轮换；应记录备份期限与恢复限制，并至少每季度演练。

## 反馈附件与最小必要

上传前在浏览器解码并重新编码，限制数量、像素、类型和大小，移除 EXIF 与非图像载荷。用户必须确认附件为本人所有或已获授权，并声明是否使用 AI 生成；没有附件时不额外收集这些声明。附件不得包含无关个人信息、凭据或其他用户数据。

## 自动清理链路

迁移 `20260809060413_compliance_release_controls.sql` 建立每日 pg_cron 任务、私有队列和可审计运行记录。任务通过 Vault 中的 `project_url` 与 `retention_secret_key` 调用 `process-feedback-retention` Edge Function；函数先从私有 Storage 删除附件，确认成功后才删除数据库反馈记录。失败任务保留并指数退避重试，不把数据库删除置于文件删除之前。

上线前必须完成：

1. 部署迁移和 Edge Function；
2. 在 Vault 配置 URL 与专用 secret API key，不得写入前端或仓库；
3. 以到期测试记录验证“入队 → 文件删除 → 数据库删除 → 审计完成”；
4. 验证失败重试、重复执行幂等性和未到期记录不受影响；
5. 持续监控 `private.feedback_retention_runs` 与失败队列。

未配置 Vault secret 时，定时任务只记录 `skipped`，不执行删除；因此仅合并代码不等于生产保留政策已经执行。

## 数据主体请求与安全事件

账户删除、访问、更正、导出、撤回同意和投诉均须有身份核验、处理记录和回复期限。确认泄露后立即限制访问、保全必要证据、评估影响，并按适用法律决定用户与主管部门通知；不得在工单、日志或公开文档复制用户敏感信息。
