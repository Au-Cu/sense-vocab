# 隐私与数据保留执行规则

版本：2026-07-29-v1

- 游客数据：仅在当前浏览器 localStorage 保存，由用户自行清除或导出。
- 账户学习数据：保存至账户删除；删除账户时通过受限 RPC 删除用户状态和反馈记录。
- 反馈文字与附件：原则上最长保存 365 天；已处理反馈目标为处理完成后 180 天内删除。
- 认证与安全日志：按 Supabase、Cloudflare 的服务策略和法律要求保存。
- 反馈图片：上传前在浏览器解码并重新编码，限制数量、像素和文件大小，不保留原文件。
- 管理员访问：应记录访问类型、管理员、目标和时间；管理员账户必须启用 MFA。
- 备份：至少每季度进行一次恢复演练；删除请求应覆盖生产数据并在备份自然轮换期内完成。
- 安全事件：确认泄露后立即限制访问、保全日志、评估影响，并按适用法律通知用户和主管部门。

后台读取反馈列表前会自动清理已满 365 天的反馈：先通过私有存储 API 删除图片，
成功后再删除数据库记录；任一步失败都会保留记录供下次重试。若需保证在到期日自动执行，
仍应在 Supabase 中配置每日定时任务或外部调度器，而不能只依赖管理员打开后台。
