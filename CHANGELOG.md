# Changelog

## 1.3.1 - 2026-08-05

- 修复学习记录增长后同步时报 `The quota has been exceeded` 的问题：本机只保存当前词书的一份权威数据，不再同时在顶层和 `bookStates` 中重复物化。
- 游客记录和各账户旧缓存会在读取时原子迁移为紧凑格式；迁移写入失败时浏览器仍保留原值，不会先删除旧记录。
- 并发版本向量由“每个标签页一个写入者”收敛为稳定的浏览器设备写入者；历史标签页向量按设备合并并保留最大计数，阻止元数据随打开次数无限增长。
- 本地容量仍不足时，登录账户的本次更新不会再被本地异常截断，而是保留在当前页面并优先完成云端同步；同步 revision 元数据也会自动退回内存保存。
- 增加旧格式压缩迁移、配额异常下云端上传、历史标签页向量合并等回归测试，并修正一个依赖固定自然日期的强化阶段测试。

兼容性说明：本次不修改云端快照结构、数据库表、词书范围、单词或义项 ID，也不批量改写计划、义项状态、热力图和学习记录。旧本机记录只改变等价的序列化布局，可继续被旧数据迁移逻辑读取。

## 1.3.0 - 2026-08-03

- 新增用户自建的易混词球体：在单词卡片展开后再次点击即可进入，支持搜索添加易混词、双向关系映射、拖动及双指旋转，并可点击球面单词跳转到对应词卡。
- 完善词卡与球体之间的连续缩放动画，消除切换闪烁和字号跳变；球面单词按远近平滑缩放与淡化，返回路径始终回到最初进入易混词模块的界面。
- 修复复习、新学、强化阶段的单词进度口径，使每个阶段只展示自身已完成/总单词数；强化阶段不再隐藏已掌握义项。
- 单词列表新增熟悉程度筛选，可与五种排序同时生效；手机端筛选占满整行，并移除词卡内重复的“返回单词列表”按钮。
- 修复重学后仍残留旧学习天数的异常记录，并保证修正后的待新学、待强化、待复习单词正常进入后续学习队列。
- 教程允许在计划窗口取消并安全返回，避免遮罩阻断操作；相关学习与同步流程增加回归覆盖。
- 消息通知中的公告改为默认只显示标题与日期，正文和图片通过右上角“展开/收起”查看。
- 后台新增公告置顶与取消置顶，置顶公告优先显示；操作受管理员校验保护并写入审计日志。

兼容性说明：本次不修改词书范围、单词或义项 ID、义项状态模型及现有学习记录。数据库迁移仅为既有公告增加默认关闭的置顶字段、排序索引和管理员置顶 RPC，不批量重写公告、已读记录或学习数据。

## 1.2.0 - 2026-07-31

- 修复完整词库加载失败时，后备小词库可能参与进度清理并将缩减后的状态同步到云端的严重问题。
- 完整词库未成功载入时暂停持久化和云同步；服务端拒绝未声明的大幅记录缩减，并在云端覆盖前保留私有恢复快照。
- 增加管理员按词书统计与学习状态恢复能力，可从最近的完整快照恢复受影响账户，不重写其他用户的记录。
- 云端与本机同时有记录时，新增双栏对比：最近学习日期、已学单词、掌握义项、待巩固义项、学习天数与当前计划均可在选择前查看。
- 新增“合并两边记录”默认操作：保留双方学习历史，同一义项冲突时采用较谨慎的状态，避免误判为已掌握。单边覆盖收入高级选项。
- 游客记录与账户记录合并时，游客端的删除痕迹不会删除云端账户内容；同一账户多设备的有效删除操作仍会正常同步。
- “重置本次标记”改为点击后立即撤销当前单词的本次标记，教程同步简化；“重学该单词”仍保留二次确认。
- 新增账户冲突合并、空白记录防覆盖、破坏性缩减拦截、恢复快照、桌面与手机对比界面等回归测试。

兼容性说明：本次不修改词书范围、单词或义项 ID、义项状态模型及现有学习记录。数据库迁移仅增加恢复快照、破坏性更新拦截和按词书的管理统计，不批量重写现有用户状态。

## 1.1.0 - 2026-07-30

- 增加轻量词库索引：首页、计划、日历和单词列表不再等待完整词库下载；完整词条在后台加载，并按内容哈希持久缓存。
- 增加清晰的词库加载、失败与重试状态，避免慢网络下先显示错误的 `0` 或出现按钮无响应；第二次访问可直接复用已缓存词库。
- 重构新用户教程遮罩定位，使高亮区域实时跟随滚动和布局变化；修复首次教程偶发不启动、账户启动延迟时漏触发等问题。
- 公告支持最多 4 张图片，上传前统一校验并重新编码；消息中心可查看图片，后台可删除公告并记录管理员审计日志。
- “消息通知”存在未读内容时，“更多”同步显示红点；历史公告继续对公告发布后注册的用户可见。
- 修复全员延长会员在数据库安全更新规则下报错的问题，并保证通知仅发给本次实际更新的账户。
- 修复“重学该单词”后学习天数沿用旧学习周期的问题：处于“待新学”的词不再显示旧周期的学习天数。
- 新增慢网络、教程重试、公告图片与删除、全员会员延长等自动化回归测试。

兼容性说明：本次仅增加词库加载索引与管理功能，不修改词书范围、单词及义项 ID、义项状态模型、已有学习记录或云端快照结构。

## 1.0.0 - 2026-07-29

- 注册流程改为一次填写邮箱和密码，再通过邮箱一次性验证码确认。
- 找回密码改为邮箱一次性验证码验证后设置新密码。
- 邀请码兑换后立即失效并自动生成新邀请码，邀请双方各延长 14 天会员。
- 管理员调整会员时长的通知改为明确显示本次延长天数。
- 保留游客本地数据、账户云同步、并发写入保护、学习记录和词书隔离。
- 增加验证码邮件模板、数据库迁移、自动化测试与生产 SMTP 部署说明。
